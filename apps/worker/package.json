{
  "name": "@llm-bench/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "ingest:livebench": "tsx src/cli.ts",
    "ingest:livebench:all": "tsx src/full-cli.ts",
    "review:livebench-aliases": "tsx src/aliases-review-cli.ts",
    "resolve:livebench-aliases": "tsx src/aliases-resolve-cli.ts",
    "sync:livebench-aliases": "tsx src/aliases-sync-cli.ts",
    "verify:livebench-aliases": "tsx src/aliases-verify-cli.ts",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@llm-bench/connectors": "workspace:*",
    "@llm-bench/db": "workspace:*",
    "drizzle-orm": "catalog:"
  },
  "devDependencies": {
    "tsx": "4.23.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
