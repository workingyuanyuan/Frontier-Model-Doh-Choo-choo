# Draft 6e591984 發布前代理審核

> 審核狀態：Agent review passed  
> 發布狀態：Not Published  
> Current Draft：`sha256:6e59198438b7515bae894aa1952523123d349c305fec44eec668a4ba69ba2b79`  
> 審核日期：2026-07-18

本文件取代 `DRAFT_REVIEW_2026-07-18.md` 對 Draft `8b9de26c…7cd228` 的審核結論。通過代理審核不會切換 Published；`Draft → Published` 仍只能由人工明確執行。

## 結論摘要

- Epoch AI、Vals AI、Artificial Analysis 的既有結構化 artifact 已有可重現的來源專用 materializer，不再依賴手寫 GPT-5.6 小樣本 fixture。
- 可排名基礎模型由 15 增至 26；Scored Profiles 由 19 增至 39；八維空值由 112/152（73.7%）降至 151/312（48.4%）。
- 仍存在 3 個 Frontier model 沒有 direct evidence；未解析的歷史／別名列保留在 CandidateResult，不會被猜測映射或誤入計分。
- Dashboard 已移除 Model 欄的 `View details`／`Viewing details`；點擊整列後，唯一高亮列與 `aria-pressed` 狀態表示目前選取模型。

## 三站物化結果

| 來源                | 原始結構化範圍                                                                                   | CandidateResult | Included／Excluded | 未解析 canonical identity |
| ------------------- | ------------------------------------------------------------------------------------------------ | --------------: | -----------------: | ------------------------: |
| Epoch AI            | 719 筆 ECI 列、771 筆 direct scored row；另有 64 個 `_external.csv` mirror 不冒充 Epoch 執行結果 |           1,231 |           771／460 |                     1,037 |
| Vals AI             | 6 個 task × 36 個非空 score，加 2 筆既有審核列                                                   |             218 |            182／36 |                       114 |
| Artificial Analysis | 29 個 model object 的非空 evaluation 欄位，加 GPT-5.6 文章 4 列                                  |             376 |            290／86 |                       162 |

Epoch ZIP 的 1,490 筆原始內部列不是 1,490 筆可計分結果：719 筆 ECI 列中只有 460 筆有有限分數，另 259 筆只有 metadata；加上 771 筆 direct scored row，正確 Candidate 數為 1,231。缺分數列不得轉成零分。

三個 materializer 連續執行兩次後，六個 Candidate／validation-report 輸出檔 SHA-256 全部不變。Candidate ID 無重複，且所有 Evidence 與欄位 provenance reference 都能解析。

## 任務 0–7

| 任務                    | 結論                  | 代理審核結果                                                                                                                                                                                    |
| ----------------------- | --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0. Draft 完整性         | PASS                  | Pointer、版本檔名、排除自我欄位後的 canonical content hash 完全一致；Published pointer 不存在。                                                                                                 |
| 1. 來源角色與採用邊界   | PASS                  | Composite 只用於選模／展示且全部 Excluded；LLM Stats 維持 index-only。三站依每筆結果分配 ORGANIZER／INDEPENDENT，不以網站單一角色覆蓋所有結果。                                                 |
| 2. 八站證據與數值       | PASS with limitations | 三站已完整物化目前 artifact 的有限結構化分數；null 不轉零、外部 mirror 不冒充執行結果。AA wider catalog、Vals benchmark detail endpoint 與 canonical alias 仍屬明載限制。                       |
| 3. 模型與 Profile 身份  | PASS                  | Product Profile 只按 effort；Harness、tools、attempt、thinking、context 不建立 Product Profile，仍保留在 Evidence provenance。                                                                  |
| 4. Benchmark 映射與衝突 | PASS                  | 新增 Finance Agent v2→Agentic、CorpFin→Context、Vibe Code Bench→Coding；Composite 保持 Excluded；同 Benchmark 衝突不重複貢獻。                                                                  |
| 5. Frontier 選模        | PASS                  | 動態 Top 20 聯集按基礎模型去重後為 29；主榜 26 個基礎模型，另 3 個等待 direct evidence。從 33 降至 29 是完整 AA payload 套用真實 Top 20 後的選模結果，不是前端截斷。                            |
| 6. 八維、排名與成本     | PASS                  | 39 個 Profile、568 筆 Product Evidence（412 Included）；380 筆唯一計分貢獻、0 重複。API standardized 圖 4 點、Measured / agent task 圖 3 點。缺失值不計零。                                     |
| 7. Dashboard 與 Preview | PASS                  | 真實 Chrome 顯示 29／26／39／3；26 列主榜，點列可更新 Selected model，Model 欄無重複 helper text。Overall 升／降排序、桌面／390px 無頁面水平溢位、console/network 無錯誤。Lighthouse 四項 100。 |

## ProductVersion 數量

- Frontier models：29
- Ranked base models：26
- Scored Profiles：39
- Awaiting direct evidence：3
- Product Evidence：568（412 Included、156 Excluded）
- 唯一計分 Evidence contribution：380（重複使用 0）

39 個 Profile 的 312 個八維格仍有 151 個 N/A：Agentic 19、Coding 8、Reasoning 13、Math 19、Knowledge 12、Language 35、Context 19、Instruction 26。這些 N/A 現在主要代表來源未測、身份尚未解析或缺乏該維 Benchmark，而不再是三站 materializer 只抄少數列的系統性缺口。

## 已知限制

- Epoch 的 1,037 筆、Vals 的 114 筆、AA 的 162 筆 Candidate 尚無 canonical identity；原始分數與來源配置已保留，未用模糊猜測強行歸屬。
- Vals detail response 的 `0.0%` server-render placeholder 不採用；目前只保留先前已審核的兩筆非 matrix Sol 列。
- Artificial Analysis artifact 只涵蓋 wider catalog 的局部 model payload；全站不能標為 FULL。
- 目前允許單維 Estimated 模型進榜，因此綜合分數的可比性仍需搭配 COV 閱讀；這符合已核准的「先顯示新模型分數、缺值不計零」規則。

## 驗證

- `@llm-bench/acquisition`：11 tests passed；typecheck、build 通過。
- `@llm-bench/benchmark-data`：28 tests passed；typecheck、build 通過。
- `@llm-bench/bench`：27 tests passed；typecheck、Next.js production static build 通過。
- Repository-wide `pnpm lint`、`pnpm format`、`pnpm typecheck`、`pnpm test:run` 與 `git diff --check` 通過。
- Chrome desktop 與 390 × 844 mobile 驗證無 document overflow；21 個 network request 全為 200／304；console 無 error、warning 或 issue。
- Lighthouse mobile snapshot：Accessibility、Best Practices、SEO、Agentic Browsing 均為 100。
- 獨立代理審核：PASS；獨立比對 5,508 個 Candidate Evidence／provenance reference、380 個計分 component 與六個來源 artifact，0 unresolved reference、0 重複計分、0 placeholder／synthetic zero 入榜。

## 人工必須處理

目前沒有資料列、來源角色、映射或 UI 問題需要人工裁決。Published 狀態切換仍只能由人工明確執行；本次工作未建立或修改 Published pointer。
