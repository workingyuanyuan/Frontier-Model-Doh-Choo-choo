pnpm-lock.yaml
.next/
apps/bench/out/
dist/
coverage/
artifacts/
apps/bench/next-env.d.ts

# Canonical machine-generated snapshots and their generated validation reports.
# Their writers guarantee deterministic serialization; Prettier must not rewrite
# immutable artifacts or create a format-only product version.
data/product/current.json
data/sources/*/candidates.json
data/sources/*/validation-report.md

# Byte-for-byte copies of artifacts entries used by the materializer tests.
# Reformatting them would change the bytes the tests parse and break the link
# back to the SHA-256 each filename records.
packages/acquisition/test-fixtures/*.html
packages/acquisition/test-fixtures/*.js
packages/acquisition/test-fixtures/*.json
packages/acquisition/test-fixtures/*.csv
packages/acquisition/test-fixtures/*.zip
